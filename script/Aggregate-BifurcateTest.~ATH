PROCREATE LOOP 1;
~ATH(LOOP) {
	PROCREATE X 1;
	PROCREATE Y 2;
	AGGREGATE [X, Y]Z;
	PROCREATE A 3;
	REPLICATE Y A;
	BIFURCATE Z[B, C];
	print ("~s, ~s", B, C);

	LOOP.DIE();
} EXECUTE(NULL);
THIS.DIE();
