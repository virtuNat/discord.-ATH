PROCREATE A 0;
FABRICATE COUNT() {
	DEBATE (A < 100) {
		PROCREATE A (A + 1);
		print ("~s ", A);
		EXECUTE (COUNT);
	}
}
PROCREATE LOOP 0;
~ATH(LOOP) {
	EXECUTE (COUNT);
	LOOP.DIE();
} EXECUTE(NULL);
THIS.DIE();
