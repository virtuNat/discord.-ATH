PROCREATE LOOP 0;
PROCREATE A 0;
FABRICATE ADDONE(){
    REPLICATE A (A + 1);
}
~ATH(LOOP) {
    EXECUTE(ADDONE);
    print ("~s ", A);
    DEBATE(A == 256) {
        print("\n");
        LOOP.DIE();
    }
}
// THIS.DIE();
