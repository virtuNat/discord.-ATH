PROCREATE X 1;
FABRICATE F(I){
  REPLICATE X I;
  AGGREGATE [I + 1, NULL]I;
  BIFURCATE I[I, NULL];
  EXECUTE(G, I);
}

FABRICATE G(J){
  AGGREGATE [J + X, NULL]J;
  BIFURCATE J[J, NULL];
}

~ATH(THIS){
  EXECUTE(F, X);
  print("~d\n", X);
  THIS.DIE();
} EXECUTE(NULL);
