PROCREATE B 5;

FABRICATE FOO() {
	REPLICATE A (B + 5);
	DIVULGATE A;
}

FABRICATE BAR() {
	PROCREATE B 2;
	EXECUTE(FOO);
}

PROCREATE LOOP 1;
~ATH(LOOP) {
	REPLICATE ANS EXECUTE(FOO);
	print ("Foo: ~d", ANS); //10
	REPLICATE ANS EXECUTE(BAR);
	print ("Bar: ~d\n", ANS); //7
	LOOP.DIE();
}
THIS.DIE();